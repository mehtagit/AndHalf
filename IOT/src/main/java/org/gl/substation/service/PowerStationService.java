package org.gl.substation.service;

import java.util.Optional;

import org.gl.substation.entity.PowerStation;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Sort;
import org.springframework.stereotype.Service;

@Service
public interface PowerStationService {
	/*
public Optional<Page<?>> list(int pageNo,int size,Sort.Direction direction);

 * public Optional<?> save(PowerStation powerStation); 
 * public Optional<?> delete(int id);
 * public Optional<?> findByID(int id);
 */
}
